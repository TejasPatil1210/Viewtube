import { VideoUrldirDirective } from './video-urldir.directive';

describe('VideoUrldirDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoUrldirDirective();
    expect(directive).toBeTruthy();
  });
});
