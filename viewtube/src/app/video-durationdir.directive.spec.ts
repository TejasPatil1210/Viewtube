import { VideoDurationdirDirective } from './video-durationdir.directive';

describe('VideoDurationdirDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoDurationdirDirective();
    expect(directive).toBeTruthy();
  });
});
